# Core dependencies
numpy>=1.20
scipy>=1.7
netCDF4>=1.5
xarray>=2022.3          # CAMS model-level reading (water-vapor correction / CAMS molecular profile)

# CAMS auto-download from the Atmosphere Data Store (calibration.io.download_cams_beta).
# Needs ADS credentials in ~/.cdsapirc. cfgrib (which pulls the eccodes wheel — the
# eccodes binary library is bundled, incl. on Windows) converts the downloaded GRIB to
# netCDF in pure Python, so no conda / no system eccodes / no grib_to_netcdf CLI is
# needed. (If the eccodes 'grib_to_netcdf' CLI happens to be on PATH it is used instead.)
cdsapi>=0.7.0
cfgrib>=0.9.10

# Dashboard (scripts/build_dashboard.py + monitoring/): indexing, Plotly figures, Jinja2 templates
pandas>=1.3
plotly>=5.0
jinja2>=3.0

# Optional: plotting
# matplotlib>=3.4

# Optional: development
# pytest>=7.0
# pytest-cov>=4.0
